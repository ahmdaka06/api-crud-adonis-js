/**
 * @module indicative-formatters
 */
import { ParsedRule } from 'indicative-parser';
import { ErrorFormatterContract } from 'indicative-compiler';
/**
 * Shape of error node
 */
export declare type JsonApiErrorNode = {
    source: {
        pointer: string;
    };
    title: string;
    detail: string;
    meta: {
        args: string[];
    };
};
/**
 * Vanilla formatter is a plain formatter to collect validation
 * errors
 */
export declare class JsonApiFormatter implements ErrorFormatterContract {
    errors: JsonApiErrorNode[];
    /**
     * Adds error to the list of existing errors
     */
    addError(error: Error | string, field: string, rule: ParsedRule['name'], args: ParsedRule['args']): void;
    /**
     * Returns an array of errors or `null` when there are no
     * errors
     */
    toJSON(): {
        errors: JsonApiErrorNode[];
    } | null;
}
