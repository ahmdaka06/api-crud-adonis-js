/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if value is a positive number or not
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * is.positive(3) // true
 * ```
 */
export declare const positive: (input: number) => boolean;
