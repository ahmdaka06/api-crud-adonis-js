/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if value is null or not.
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * if (is.isNull(null)) {
 * }
 * ```
 */
export declare const isNull: (input: any) => boolean;
