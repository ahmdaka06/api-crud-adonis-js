/**
 * @module indicative-rules
 */
import { DateOffsetKeys } from '../Contracts';
/**
 * Returns a boolean telling if input date is after the given
 * offset or not.
 *
 * The `key` has to be one of the following values.
 *
 * - years
 * - quarters
 * - months
 * - weeks
 * - days
 * - hours
 * - minutes
 * - seconds
 * - milliseconds
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * if (is.afterOffsetOf(new Date(), 10, 'days')) {
 * }
 * ```
 */
export declare const afterOffsetOf: (input: string | number | Date, diffUnit: number, key: DateOffsetKeys) => boolean;
