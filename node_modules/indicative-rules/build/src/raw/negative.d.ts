/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if value is a negative number or not
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * is.negative(-3) // true
 * ```
 */
export declare const negative: (input: number) => boolean;
