/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if email is valid as per email regex pattern
 * or not.
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * if (is.email('virk@adonisjs.com')) {
 * }
 * ```
 */
export declare const email: (input: string) => boolean;
