/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if value is even or not
 *
 * ```js
 * const { is } = require('indicative')
 *
 * is.even(2) // true
 * is.even(3) // false
 * ```
 */
export declare const even: (input: number) => boolean;
