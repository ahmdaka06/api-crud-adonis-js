/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if value is an ip address or not.
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * if (is.ip('127.0.0.1')) {
 * }
 * ```
 */
export declare const ip: (input: string) => boolean;
