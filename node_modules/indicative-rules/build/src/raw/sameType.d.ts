/**
 * @module indicative-rules
 */
/**
 * Compare two values to be exactly same type
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * is.same('foo', 'bar') // true
 * is.same(10, 22) // true
 * ```
 */
export declare const sameType: (input: any, comparsionInput: any) => boolean;
