/**
 * @module indicative-rules
 */
/**
 * Returns a boolean telling if number is an odd number or not.
 *
 * @example
 * ```js
 * const { is } = require('indicative')
 *
 * is.odd(3) // true
 * ```
 */
export declare const odd: (input: number) => boolean;
