"use strict";
/*
* indicative-rules
*
* (c) Harminder Virk <virk@adonisjs.com>
*
* For the full copyright and license information, please view the LICENSE
* file that was distributed with this source code.
*/
Object.defineProperty(exports, "__esModule", { value: true });
const indicative_utils_1 = require("indicative-utils");
const truthy_1 = require("../../raw/truthy");
/**
 * Enforces the field under validation is accepted. Empty strings, `false`, `null`,
 * `0` and `undefined` values will be considered as not accepted.
 *
 * ```ts
 * import { validations } from 'indicative/validator'
 *
 * const rules = {
 *   terms: 'accepted'
 * }
 *
 * // or
 * const rules = {
 *   terms: [
 *     validations.accepted()
 *   ]
 * }
 * ```
 */
const validation = {
    async: false,
    validate: (data, field, _args, config) => {
        const fieldValue = indicative_utils_1.getValue(data, field);
        return indicative_utils_1.skippable(fieldValue, field, config) || truthy_1.truthy(fieldValue);
    },
};
exports.default = validation;
