"use strict";
/*
* indicative-rules
*
* (c) Harminder Virk <virk@adonisjs.com>
*
* For the full copyright and license information, please view the LICENSE
* file that was distributed with this source code.
*/
Object.defineProperty(exports, "__esModule", { value: true });
const indicative_utils_1 = require("indicative-utils");
const isObject_1 = require("../../raw/isObject");
/**
 * Enforces the field value to be a valid Javascript object.
 *
 * The validation will fail for `Arrays`, though they are objects too in Javascript.
 *
 * ```ts
 * import { validations } from 'indicative/validator'
 *
 * const rules = {
 *   user: 'object'
 * }
 *
 * // or
 * const rules = {
 *   user: [
 *     validations.object()
 *   ]
 * }
 * ```
 */
const validation = {
    async: false,
    validate: (data, field, _args, config) => {
        const fieldValue = indicative_utils_1.getValue(data, field);
        return indicative_utils_1.skippable(fieldValue, field, config) || isObject_1.isObject(fieldValue);
    },
};
exports.default = validation;
