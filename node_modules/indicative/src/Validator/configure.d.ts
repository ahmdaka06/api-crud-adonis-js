import { ValidatorConfig } from '../Contracts';
/**
 * Configure global validation options
 */
export declare function configure(config: Partial<ValidatorConfig>): void;
export declare namespace configure {
    var DEFAULTS: ValidatorConfig;
}
